# ---------------------------------------------------------------------------
# PubliBot — dependencias de desenvolvimento e teste
#
# Uso:  pip install -r requirements.txt -r requirements-dev.txt
# ---------------------------------------------------------------------------

-r requirements.txt

# --- Broker Celery sobre PostgreSQL (modo de desenvolvimento) ---------------
# Necessario apenas quando CELERY_BROKER=postgres, que permite desenvolver sem
# um Redis instalado — util sobretudo no Windows, onde nao existe build oficial
# do Redis. Producao usa Redis e nao instala isto. Ver docs/adr/ADR-0013.
SQLAlchemy==2.0.52

# --- Testes ----------------------------------------------------------------
pytest==8.4.2
pytest-django==4.11.1
pytest-cov==6.2.1

# --- Lint e formatacao -----------------------------------------------------
ruff==0.14.2

# --- Hooks de pre-commit ---------------------------------------------------
pre-commit==4.3.0

# --- Leitura de YAML nos testes --------------------------------------------
# `tests/test_deploy_scripts.py` le o workflow do GitHub Actions como dado, e
# nao como texto: e a unica forma de afirmar que o job de implantacao esta
# preso a `main`. Chega junto com o pre-commit de qualquer jeito; declarado
# aqui para a suite nao depender de dependencia transitiva alheia.
PyYAML==6.0.3

# --- Servicos de GPU, para a suite conseguir exercita-los -------------------
# `worker-gpu/` tem o proprio requirements e o proprio venv: ele traz torch e
# nao entra no da nuvem. Mas os servicos de la sao FastAPI, e sem o FastAPI
# aqui nenhum teste conseguia sequer importa-los — o resultado era um servico
# que recebe credencial, decide 503 e responde ao PubliBot sem um unico teste.
#
# So o FastAPI, e nao o resto: o que se testa e o HTTP (credencial, recusa
# quando ocupado, formato da resposta), com a parte pesada substituida. Torch e
# diffusers continuam fora daqui.
fastapi==0.121.2
